<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Med-AI Report</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        box-sizing: border-box;
      }
      .container {
        max-width: 600px;
        margin: auto;
        padding: 20px;
        border: 1px solid #000;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      h1 {
        text-align: center;
        text-decoration: underline;
      }
      label {
        display: block;
        margin: 1rem;
      }

      .signature {
        margin-top: 20px;
      }
      .signature div {
        display: inline-block;
        width: 45%;
        vertical-align: top;
      }
      .signature div:first-child {
        width: 50%;
      }
      table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Medical Report - Med-AI</h1>
      <form>
        <label for="text">Date : {{date}}</label>

        <label for="name">Patient Name : {{name}}</label>

        <label for="address">Address : {{address}}</label>

        <label>Sex : {{sex}}</label>

        <label>Marital State: {{marital_status}}</label>

        <label for="blood_type">Blood Type: {{bloodType}}</label>

        <h4>Patient Prescription </h4>

        <table>
          <tr>
            <th>Dose</th>
            <th>Medicine Name</th>
            <th>Time</th>
          </tr>
          {{#each dosages}}
          <tr>
            <td>{{this.medDose}}</td>
            <td>{{this.medName}}</td>
            <td>{{this.time}}</td>
          </tr>
          {{/each}}
         
        </table>
      </form>
      <footer style="margin-top: 5rem;">Report generated by Med-AI</footer>
    </div>
  </body>
</html>
